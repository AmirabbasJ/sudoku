// import axios from 'axios';

// import type { Board, Difficulty } from '../core';

// export const fetchSudoku = (diff: Difficulty) =>
//   axios.get(`api/sudoku/${diff}`).then(({ data }) => data.sudoku as Board);
export {};
